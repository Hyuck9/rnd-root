bootJar{
    enabled = false;
}
jar {
    enabled = true;
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    compile(project(':rnd-common'))
    compile(project(':rnd-lib-database'))

    testCompile group: 'junit', name: 'junit', version: '4.12'
}
